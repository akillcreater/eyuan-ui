/**
 * 组件尺寸类型
 * 用于定义组件的大小规格
 *
 * - `tiny`: 超小尺寸，适用于紧凑的布局场景
 * - `small`: 小尺寸，略小于默认大小
 * - `medium`: 中尺寸，默认尺寸
 * - `large`: 大尺寸，比默认尺寸更大
 * - `giant`: 超大尺寸，适用于需要突出显示的场景
 */
export type ComponentSize = 'tiny' | 'small' | 'medium' | 'large' | 'giant'
