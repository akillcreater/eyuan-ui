/**
 * 组件变体类型
 * 用于定义组件的不同视觉样式风格
 *
 * - `filled`: 填充样式，具有实心的背景色
 * - `tinted`: 淡色样式，具有半透明或浅色的背景色
 * - `outlined`: 轮廓样式，仅显示边框无背景色
 * - `text`: 文本样式，无背景色和边框，类似普通文本
 * - `icon`: 图标样式，专为图标设计的样式
 */
export type ComponentVariant = "filled" | "tinted" | "outlined" | "text" | "icon"
